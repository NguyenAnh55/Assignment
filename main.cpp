#include <iostream>
#include "house.h"
#include <member.h>
#include <vector>

using std::cout;
using std::string;
using std::vector;

void searchHouse (Member a, vector<House> b){
    for (size_t i = 0; i < b.size(); i++)
    {
        if (a -> occupier_rate >= num -> -> rating_score && a -> credit >= b ->house_credit  && b -> status == true)
        {
            cout << "The house with ID" << b -> id << " is suitable\n"
        } 
    }
}

int main ()
{

}